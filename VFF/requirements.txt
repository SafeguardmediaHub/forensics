# VideoForensics System — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core Video Processing ──────────────────────────────────────────────────────
opencv-python>=4.8.0          # Frame extraction, optical flow, image analysis
ffmpeg-python>=0.2.0          # FFmpeg bindings for container/stream analysis
pymediainfo>=6.0.1            # MediaInfo bindings for rich metadata extraction

# ── Signal Processing & Scientific Computing ──────────────────────────────────
numpy>=1.24.0                 # Array operations — used everywhere
scipy>=1.11.0                 # Signal processing, statistical analysis
scikit-image>=0.21.0          # Image analysis, noise estimation
Pillow>=10.0.0                # Image I/O and manipulation

# ── Audio Analysis ────────────────────────────────────────────────────────────
librosa>=0.10.0               # Audio feature extraction, spectral analysis
soundfile>=0.12.1             # Audio I/O, lossless read/write

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.3.0           # Classical ML, anomaly detection, calibration

# ── Metadata Extraction ───────────────────────────────────────────────────────
ExifRead>=3.0.0               # EXIF metadata parsing
hachoir>=3.2.0                # Binary file format parsing

# ── Data & Storage ────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0             # Database ORM
alembic>=1.12.0               # Database migrations

# ── API Layer ─────────────────────────────────────────────────────────────────
fastapi>=0.104.0              # REST API framework
uvicorn[standard]>=0.24.0     # ASGI server
python-multipart>=0.0.6       # File upload handling

# ── Report Generation ─────────────────────────────────────────────────────────
reportlab>=4.0.0              # PDF report generation
jinja2>=3.1.0                 # Report templating
matplotlib>=3.8.0             # Chart generation for reports

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0          # Environment variable management
pydantic>=2.4.0               # Data validation
tqdm>=4.66.0                  # Progress bars for long operations
click>=8.1.0                  # CLI interface

# ── Security ──────────────────────────────────────────────────────────────────
cryptography>=41.0.0          # Encryption for stored files
python-jose>=3.3.0            # JWT for API authentication

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
